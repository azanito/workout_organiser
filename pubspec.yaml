name: workout_organiser
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:

  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.17.5
  firebase_ui_auth: ^1.14.0
  google_sign_in: ^6.1.5
  
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^4.0.0


  flutter:
    sdk: flutter

  # локализация
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0              # ← обновлён до 0.19.x

  # state management
  provider: ^6.0.5

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
